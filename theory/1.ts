const firstName:string = 'Max';
const age:number = 25;
const isOwner:boolean = true;
const profile:undefined = undefined;
const city:null = null;
const isHasGf:unknown = null;
const dogName:any = true;

const userProfile: {
  firstName: string,
  age: number,
  isOwner: boolean
} = {
  firstName: 'Max',
  age: 25,
  isOwner: true
}

const numbers:number[] = [1,2,3,4];
const numbers2:[number, number] = [1,2];
const numbers3:[string, number] = ['Max', 2];
